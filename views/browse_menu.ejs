<div class="container mx-auto px-4 py-8">
  <!-- Deals Section -->
  <h2 class="text-2xl font-bold">Up to -40% 🎊 Order.uk exclusive deals</h2>

  <!-- Deals Cards -->
  <div class="grid grid-cols-3 gap-4 mt-6">
    <% let deals = [
      { name: "Chef Burgers London", discount: "-40%", img: "/assets/Chef Burgers.png" },
      { name: "Grand Ai Cafe London", discount: "-20%", img: "/assets/Grand Ai.png" },
      { name: "Butterbrot Café London", discount: "-17%", img: "/assets/Butterbrot.png" }
    ]; %>

    <% deals.forEach(deal => { %>
      <div class="relative rounded-xl overflow-hidden">
        <img src="<%= deal.img %>" alt="<%= deal.name %>" class="w-full h-full object-cover">
      </div>
    <% }) %>
  </div>

  <!-- Popular Categories -->
  <h2 class="text-2xl font-bold mt-10">Order.uk Popular Categories 🤩</h2>
  <div class="grid grid-cols-6 gap-4 mt-6">
    <% let categories = [
      { name: "Burgers & Fast food", count: 21, img: "/assets/burger.png" },
      { name: "Fries", count: 32, img: "/assets/fries.png" },
      { name: "Drinks", count: 4, img: "/assets/drinks.png" },
      { name: "Pizza", count: 32, img: "/assets/pizza.png" },
      { name: "Breakfast", count: 4, img: "/assets/breakfast.png" },
      { name: "Soups", count: 32, img: "/assets/soup.png" }
    ]; %>

    <% categories.forEach(category => { %>
      <div class="bg-gray-100 rounded-xl text-center shadow">
        <img src="<%= category.img %>" alt="<%= category.name %>" class="w-full h-44 object-cover rounded-lg">
        <div class="p-4">
          <h3 class="font-bold text-lg mt-2"><%= category.name %></h3>
          <p class="text-gray-500 text-sm"><%= category.count %> Restaurants</p>
        </div>
      </div>
    <% }) %>
  </div>
</div>
